[package]
name = "netsentinel-capture-common"
version = "0.1.0"
edition.workspace = true

license.workspace = true

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = "0.13", optional = true }

[lib]
path = "src/lib.rs"
